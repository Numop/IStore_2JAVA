<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="IStore.controller.WhitelistController"
            stylesheets="@../css/styles.css" styleClass="main-container">

    <!-- Header -->
    <top>
        <HBox spacing="20" alignment="CENTER_LEFT"
              style="-fx-background-color: #1a1a24; -fx-padding: 16 24; -fx-border-color: #252532; -fx-border-width: 0 0 1 0;">
            <Button text="← Retour" onAction="#handleBack" styleClass="header-button"/>
            <VBox spacing="2">
                <Label text="📧 Whitelist des emails" style="-fx-text-fill: white; -fx-font-size: 20; -fx-font-weight: bold;"/>
                <Label text="Gérer les emails autorisés à s'inscrire" style="-fx-text-fill: #6b7280; -fx-font-size: 12;"/>
            </VBox>
        </HBox>
    </top>

    <!-- Content -->
    <center>
        <VBox spacing="24" style="-fx-padding: 24;">

            <!-- Info Card -->
            <HBox styleClass="card" style="-fx-background-color: rgba(99,102,241,0.1); -fx-border-color: #6366f1;"
                  spacing="16" alignment="CENTER_LEFT">
                <Label text="Seuls les utilisateurs dont l'email est dans cette liste peuvent créer un compte sur iStore."
                       wrapText="true" style="-fx-text-fill: #9ca3af;"/>
            </HBox>

            <!-- Add Email Card -->
            <VBox styleClass="card" spacing="16">
                <Label text="Ajouter un email" style="-fx-text-fill: white; -fx-font-weight: bold; -fx-font-size: 16;"/>
                <HBox spacing="12" alignment="CENTER_LEFT">
                    <TextField fx:id="emailField" promptText="exemple@email.com" prefHeight="42" HBox.hgrow="ALWAYS"/>
                    <Button text="➕ Ajouter" onAction="#handleAddEmail" styleClass="primary-button" prefHeight="42"/>
                </HBox>
            </VBox>

            <!-- Email List -->
            <VBox VBox.vgrow="ALWAYS" spacing="12">
                <Label text="📋 Emails autorisés" style="-fx-text-fill: white; -fx-font-weight: bold; -fx-font-size: 16;"/>

                <VBox VBox.vgrow="ALWAYS" styleClass="card" style="-fx-padding: 0;">
                    <ListView fx:id="whitelistView" VBox.vgrow="ALWAYS"/>
                </VBox>

                <Button text="➖ Supprimer l'email sélectionné" onAction="#handleRemoveEmail"
                        styleClass="danger-button"/>
            </VBox>

        </VBox>
    </center>

</BorderPane>
