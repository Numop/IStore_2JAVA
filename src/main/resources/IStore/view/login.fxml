<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<StackPane xmlns:fx="http://javafx.com/fxml"
           fx:controller="IStore.controller.LoginController"
           stylesheets="@../css/styles.css" styleClass="main-container"
           prefWidth="900" prefHeight="650">

    <VBox alignment="CENTER" spacing="0">

        <!-- Card de connexion -->
        <VBox styleClass="login-card" spacing="28" maxWidth="400" alignment="CENTER">

            <!-- Logo / Titre -->
            <VBox alignment="CENTER" spacing="8">
                <Label text="🏪" style="-fx-font-size: 48;"/>
                <Text text="iStore" styleClass="app-title">
                    <font>
                        <Font size="32" name="System Bold"/>
                    </font>
                </Text>
                <Text text="Connectez-vous à votre compte" styleClass="subtitle">
                    <font>
                        <Font size="14"/>
                    </font>
                </Text>
            </VBox>

            <!-- Formulaire -->
            <VBox spacing="16">

                <!-- Email -->
                <VBox spacing="8">
                    <Label text="Adresse email" style="-fx-text-fill: #9ca3af; -fx-font-size: 13; -fx-font-weight: bold;"/>
                    <TextField fx:id="emailField" promptText="exemple@email.com" prefHeight="44"/>
                </VBox>

                <!-- Mot de passe -->
                <VBox spacing="8">
                    <Label text="Mot de passe" style="-fx-text-fill: #9ca3af; -fx-font-size: 13; -fx-font-weight: bold;"/>
                    <PasswordField fx:id="passwordField" promptText="••••••••" prefHeight="44"/>
                </VBox>

            </VBox>

            <!-- Bouton connexion -->
            <VBox spacing="16">
                <Button text="Se connecter" onAction="#handleLogin" styleClass="primary-button"
                        prefWidth="340" prefHeight="44"/>

                <Separator/>

                <!-- Lien inscription -->
                <HBox alignment="CENTER" spacing="8" style="-fx-padding: 0 0 16 0;">
                    <Label text="Pas encore de compte ?" styleClass="subtitle"/>
                    <Button text="Créer un compte" onAction="#handleRegister" styleClass="link-button"/>
                </HBox>
            </VBox>

        </VBox>

    </VBox>

</StackPane>

